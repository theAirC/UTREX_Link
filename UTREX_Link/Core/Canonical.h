#pragma once

typedef s32 Delta;      // Relative values, like mouse movement or trackball change.
typedef u16 Range;      // Absolute values, like throttles sticks, knobs and joysticks.
typedef u8 State;       // Distinct state values, like multi-position switches and D-pads.
typedef Bits Buttons_t; // Simple buttons, 1 or 0
